# Copilot Instructions

## Project Guidelines
- Space Engineers mod script compiler uses C# 6. All mod code must avoid C# 7+ features: throw expressions, pattern matching (is Type var), out var declarations, expression-bodied property accessors with complex logic, switch on non-primitive types.